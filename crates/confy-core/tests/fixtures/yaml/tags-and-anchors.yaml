---
defaults: &defaults
  adapter: postgres
  host: localhost

development:
  <<: *defaults
  database: dev_db

explicit_str: !!str 123
custom_tag: !Custom
  field: value

binary: !!binary |
  R0lGODlhDAAMAIQAAP

aliased_list: &items
  - a
  - b
reused: *items
