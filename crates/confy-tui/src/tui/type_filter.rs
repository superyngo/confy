// Re-exported from confy-core — all type-filter state and logic live there.
pub use confy_core::session::type_filter::{
    Cell, CheckState, Group, LayoutRow, TypeFilter, TypeToken, classify, layout, nav_rows,
};
